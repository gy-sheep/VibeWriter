커밋을 진행한다. 아래 순서를 따른다.

1. `docs/git/COMMIT_CONVENTION.md`를 읽어 커밋 메시지 규칙을 확인한다.
2. `git status`와 `git diff`를 실행해 변경 사항을 파악한다.
3. 규칙에 맞는 커밋 메시지를 작성하고 사용자에게 보여준 뒤 승인을 받는다.
4. 승인되면 커밋을 실행한다.
5. 커밋 완료 후 push 여부를 사용자에게 확인한다.
6. push 승인 시 현재 브랜치 기준으로 `git push`를 실행한다.

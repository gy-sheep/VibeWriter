# Phase {N} · Step {N} 개발 기획

> **범위**: {이 Step이 다루는 기능 영역}
> **목표**: {입력→처리→출력을 한 줄로 요약}

---

## 1. 구현 범위

| Step | 기능 | 담당 모듈 |
|------|------|----------|
| Step N | {기능 설명} | `{module/path.py}` |

---

## 2. 데이터 흐름

```
{입력 파일/데이터}
        │
        ▼ ({처리 설명})
  [{Agent 또는 모듈}]
        │ {처리 결과}
        ▼
  {출력 파일/데이터}
```

---

## 3. 파일 명세

### 입력

**`{입력 파일 경로}`**
```json
{입력 파일 예시}
```

### 출력

**`{출력 파일 경로}`**
```json
{출력 파일 예시}
```

- `{필드명}`: {필드 설명 및 생성 규칙}

---

## 4. 모듈 상세

### `{utils 또는 agents}/{module.py}`

- `{함수명}({인자})` → {반환값}: {동작 설명}
- {주요 처리 로직 또는 규칙}
- {예외/실패 처리 방식}

### `{agents}/{agent.py}` — {AgentName}

- {핵심 로직 설명}
- {LLM 사용 시: 프롬프트 설계 방침}
- {중복/실패 처리 방침}

---

## 5. 구현 후 프로젝트 구조

```
VibeWriter/
├── agents/
│   └── {변경·추가 파일}    ← {역할 설명} (신규 또는 수정)
├── utils/
│   └── {변경·추가 파일}    ← {역할 설명} (신규 또는 수정)
├── data/
│   └── {신규 디렉터리}/    ← {용도} (.gitignore 적용 여부)
└── docs/dev/
    └── phase{N}-step{N}.md ← 본 문서
```

---

## 6. 검증 방법

| 항목 | 확인 방법 |
|------|----------|
| {검증 항목 1} | {확인 방법} |
| {검증 항목 2} | {확인 방법} |

# VibeWriter — 전체 파이프라인 개요

> 이 문서는 VibeWriter의 두 파이프라인(학습 / 생성)이 어떤 흐름으로 동작하는지,
> 각 Agent가 무슨 역할을 하는지 한눈에 파악하기 위한 가이드입니다.

---

## 전체 구조 한눈에 보기

```
VibeWriter
├── [학습 파이프라인]  python main.py learn
│     블로그 URL → 스타일 분석 → 스타일 가이드 저장
│
└── [생성 파이프라인]  python main.py write --topic "주제"
      주제 입력 → 팩트 수집 → 아웃라인 → 본문 생성 → 품질 검증 → 최종 파일
```

---

## 1. 학습 파이프라인

> 목표: 내 블로그 글들을 읽고 "나만의 글쓰기 스타일"을 추출해 저장한다.

```
data/input/blog_urls.txt
         │
         ▼
┌──────────────────────────────────────────────────────────┐
│  Step 1. CrawlerAgent  (agents/crawler.py)               │
│  - blog_urls.txt에서 URL 목록 읽기                        │
│  - httpx로 HTML 수집, 실패 시 1회 재시도                  │
│  - 네이버 블로그 iframe 구조 자동 처리                     │
│  - 완료된 URL에 "# done" 마킹                             │
└───────────────────────┬──────────────────────────────────┘
                        │ data/raw_html/{slug}.html
                        ▼
┌──────────────────────────────────────────────────────────┐
│  Step 2. ParserAgent  (agents/parser.py)                 │
│  - BeautifulSoup4으로 본문 텍스트 추출                    │
│  - 광고·메뉴·노이즈 제거                                  │
│  - 네이버 스마트에디터 선택자 처리                         │
└───────────────────────┬──────────────────────────────────┘
                        │ data/parsed_posts/{slug}.json
                        ▼
┌──────────────────────────────────────────────────────────┐
│  Step 3. AnalysisAgent  (agents/analysis.py)             │
│  - [로컬 LLM] 글 내용으로 카테고리 자동 분류              │
│    └─ 허용 목록(config.py)에서만 선택, 없으면 "etc"       │
│  - [로컬 LLM] 문체·어휘·구조 패턴 분석                   │
│    └─ 경어/반말, 문장 길이, 자주 쓰는 단어, 단락 구성 등  │
└───────────────────────┬──────────────────────────────────┘
                        │ data/analysis/{slug}.json
                        ▼
┌──────────────────────────────────────────────────────────┐
│  Step 4. StyleGuideAgent  (agents/style_guide.py)        │
│  - 카테고리별 analysis 파일 집계                          │
│  - 카테고리별 스타일 가이드 Markdown 생성                  │
│  - 기존 가이드 있을 경우 중복 제거 후 병합 업데이트         │
└───────────────────────┬──────────────────────────────────┘
                        │ data/style_guides/{category}.md
                        ▼
                  [학습 완료 — 스타일 가이드 저장됨]
```

**핵심**: 이 파이프라인은 한 번만 실행하면 되고, 새로운 글을 추가할 때만 재실행한다.

---

## 2. 생성 파이프라인

> 목표: 주제를 입력하면 학습된 스타일로 사실 기반의 블로그 글을 자동 생성한다.

```
python main.py write --topic "맥북 프로 M4 실사용 후기"
         │
         ▼
┌──────────────────────────────────────────────────────────┐
│  Step 1. ResearchAgent  (agents/researcher.py)  🆕 예정  │
│                                                          │
│  [로컬 LLM] 주제에서 10개 검색 쿼리 자동 생성             │
│       예) "M4 성능 벤치마크", "M4 발열 특성", ...         │
│                    │                                     │
│  [Gemini API 무료] 각 쿼리로 응답 수집 (10회 호출)         │
│       └─ 최신 정보·사실 기반 응답 획득                    │
│                    │                                     │
│  [로컬 LLM] 10개 응답 요약·중복 제거·핵심 팩트 선별        │
└───────────────────────┬──────────────────────────────────┘
                        │ data/output/{slug}_research.json
                        ▼
┌──────────────────────────────────────────────────────────┐
│  Step 2. PlannerAgent  (agents/planner.py)               │
│  - 주제 키워드 추출                                       │
│  - 카테고리 자동 추론 → 해당 스타일 가이드 로드            │
│  - [로컬 LLM] 팩트 컨텍스트 + 스타일 가이드 기반          │
│    SEO 제목 후보 생성 + 섹션별 아웃라인 구성              │
└───────────────────────┬──────────────────────────────────┘
                        │ data/output/{slug}_outline.json
                        ▼
┌──────────────────────────────────────────────────────────┐
│  Step 3. WriterAgent  (agents/writer.py)                 │
│  - 아웃라인 + 스타일 가이드 + 팩트 컨텍스트 로드           │
│  - [로컬 LLM] 섹션 단위로 본문 생성                       │
│    └─ 이전 섹션 문맥(최대 200자) 유지하며 연속성 확보      │
│  - 제목·메타 주석·섹션 조합 → draft.md 저장               │
└───────────────────────┬──────────────────────────────────┘
                        │ data/output/{slug}_draft.md
                        ▼
┌──────────────────────────────────────────────────────────┐
│  Step 4. QualityAgent  (agents/quality.py)               │
│                                                          │
│  [코드] humanize.py 규칙 적용                             │
│    - 외국 문자(한자·키릴·아랍 등) 제거                    │
│    - AI 과잉 표현 제거 ("물론입니다", "매우 중요합니다" 등) │
│    - 반복 접속사 탐지·교체                                │
│    - 반복 어구(bi-gram) 탐지                              │
│                                                          │
│  [코드] 스타일 체크 4항목                                 │
│    - 문장 길이 단조로움                                   │
│    - AI 표현 잔존 여부                                    │
│    - 반복 접속사·반복 어구                                │
│                                                          │
│  [로컬 LLM] 섹션별 polish                                │
│    - 맞춤법·반복 표현·문장 리듬 다듬기                    │
│    - 길이 이탈(±40%) 시 원본 fallback                     │
└───────────────────────┬──────────────────────────────────┘
                        │ data/output/{slug}_final.md
                        ▼
                   [생성 완료 — 블로그 글 저장됨]
```

---

## 3. Agent 역할 요약

| Agent | 파일 | 사용 LLM | 주요 역할 |
|-------|------|----------|----------|
| CrawlerAgent | `agents/crawler.py` | 없음 | URL 크롤링, HTML 수집 |
| ParserAgent | `agents/parser.py` | 없음 | 본문 추출, 노이즈 제거 |
| AnalysisAgent | `agents/analysis.py` | 로컬 Ollama | 카테고리 분류, 문체 분석 |
| StyleGuideAgent | `agents/style_guide.py` | 없음 | 스타일 가이드 집계·생성 |
| **ResearchAgent** | `agents/researcher.py` | **Gemini + 로컬** | **팩트 수집·요약** (🆕 예정) |
| PlannerAgent | `agents/planner.py` | 로컬 Ollama | 아웃라인 구성, 제목 생성 |
| WriterAgent | `agents/writer.py` | 로컬 Ollama | 섹션별 본문 생성 |
| QualityAgent | `agents/quality.py` | 로컬 Ollama | humanize, polish |

---

## 4. ResearchAgent 역할 상세 (신규 추가 예정)

```
[주제 입력]
     │
     ├─ [로컬 Ollama]  주제 → 10개 검색 쿼리 생성
     │       예) ["M4 Pro 벤치마크", "M4 발열 문제", "M4 배터리 수명", ...]
     │
     ├─ [Gemini API]   10개 쿼리 순차 호출 → 각 응답 수집
     │       무료 한도: 하루 1,500 요청 / 분당 15 요청
     │
     ├─ [로컬 Ollama]  10개 응답 → 중복 제거 + 핵심 팩트 요약
     │
     └─ research.json 저장
           └─ PlannerAgent / WriterAgent에 컨텍스트로 전달
```

**Gemini가 하는 것**: 최신 사실 정보 조회 (검색 역할)
**로컬 LLM이 하는 것**: 어떤 질문을 할지 판단 + 수집된 정보 분석·선별·요약

> Gemini도 LLM 기반이므로 100% 팩트 보장은 아니지만,
> 순수 로컬 모델 단독 생성 대비 사실 기반 콘텐츠 품질이 크게 향상된다.

---

## 5. 데이터 흐름 전체 요약

```
[학습]
blog_urls.txt
    → raw_html/         (CrawlerAgent)
    → parsed_posts/     (ParserAgent)
    → analysis/         (AnalysisAgent)
    → style_guides/     (StyleGuideAgent)

[생성]
주제 입력
    → output/{slug}_research.json   (ResearchAgent)  🆕 예정
    → output/{slug}_outline.json    (PlannerAgent)
    → output/{slug}_draft.md        (WriterAgent)
    → output/{slug}_final.md        (QualityAgent)
```

---

## 6. 개발 Phase 현황

| Phase | 내용 | 상태 |
|-------|------|------|
| Phase 1 | 학습 파이프라인 (크롤링 → 스타일 가이드) | ✅ 완료 |
| Phase 2 | 생성 파이프라인 (아웃라인 → 본문 → 품질 검증) | ✅ 완료 |
| **ResearchAgent** | **팩트 수집 단계 (Gemini + 로컬 LLM)** | **🔲 다음 작업** |
| Phase 3 | 웹 인터페이스 (FastAPI + Jinja2) | 🔲 예정 |
| Phase 4 | 고도화 (다중 프로필, 자동 업로드 등) | 🔲 추후 |
